Encoding = require('./pages/encoding_page.coffee')

describe 'Encoding convert page', ->

  it 'has a link to message digest tool.'

  it 'has a link to crypt tool.'

  describe 'can select source encoding', ->

    describe 'and select target encoding', ->
      it 'should be'

    describe 'and use default target encoding', ->
      it 'dummy', ->
        expect(true).to.equals(true)

  describe 'can select target encoding', ->

    describe 'and use default source encoding.', ->
      it 'dummy', ->
        expect(true).to.equals(true)
